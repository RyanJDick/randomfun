# go-htmx-tailwind

A web app template built with:
- GO
- HTMX
- TailwindCSS

## Quick Start

Build and run with:
```bash
# Run vite build from (from /frontend).
pnpm build

# Build and run the app (from /).
go run ./cmd/app
```
